import styled from 'styled-components';

const VideoContainer = styled.div`
  position: relative;
  height: 100vh;
  width: 100vw;
  background: #333;
  overflow: hidden;
`;

export default VideoContainer;
